{
  "hooks": [
    {
      "event": "SessionStart",
      "type": "prompt",
      "prompt": "At the start of this session, gather project context:\n\n1. Run `git status` to see current branch and uncommitted changes\n2. Run `git log --oneline -5` to see recent commits\n3. Look for TODO, FIXME, or HACK comments in recently modified files\n4. Identify the project type and tech stack from configuration files\n\nProvide a brief summary of the project state to establish context.",
      "timeout": 30000
    },
    {
      "event": "PreToolUse",
      "matcher": "Write|Edit",
      "type": "prompt",
      "prompt": "Before writing or editing code, perform a quick validation:\n\n1. Check if the code being written contains any hardcoded secrets (API keys, passwords, tokens)\n2. Verify the code follows basic style guidelines\n3. Check for obvious security issues (SQL injection patterns, command injection)\n\nIf any issues are found, warn about them before proceeding. If the issues are critical (like hardcoded secrets), recommend changes.\n\nKeep this check fast - only flag obvious issues.",
      "timeout": 10000
    },
    {
      "event": "PostToolUse",
      "matcher": "Write|Edit",
      "type": "prompt",
      "prompt": "After code was written or edited, provide brief feedback:\n\n1. Note if the code would benefit from formatting (Black for Python)\n2. Mention if there are obvious improvements (missing type hints, unclear names)\n3. Suggest if tests should be added for the new code\n\nKeep feedback brief and actionable. Don't repeat issues already addressed.",
      "timeout": 10000
    },
    {
      "event": "Notification",
      "type": "command",
      "command": "echo 'Task notification received'",
      "timeout": 5000
    }
  ]
}